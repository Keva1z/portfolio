# 🚀 Портфолио Вячеслава (Keva1z)

**Демонстрация backend-разработки с AI-сгенерированным фронтенд-ом**

Это портфолио показывает навыки backend-разработки. Фронтенд был создан с помощью нейросети — фокус на функциональность и архитектуру, а не на дизайн. 

## 📋 О проекте

- **Frontend**: React 18 + Vite + Tailwind CSS (сгенерирован AI)
- **Backend**: FastAPI, NestJS, Express (готов для реализации)
- **Особенности**:
  - Многоэкранная презентация с навигацией по клавиатуре
  - Фиксированный видеофон с анимацией
  - Компонент автоматической загрузки мыслей из Telegram-бота
  - Полная адаптивность под мобильные устройства
  - Tech stack: Python (FastAPI, Aiogram, SQLAlchemy, Pytest), TypeScript (NestJS, Express, Prisma), DevOps (Git, Docker, Nginx)

## 🛠️ Стек технологий

### Frontend
- **React 18** — UI компоненты
- **Vite** — быстрая сборка и разработка
- **Tailwind CSS** — утилитарные стили
- **CSS Custom** — анимации и переходы

### Backend (готовы к реализации)
- **Python**: FastAPI, Aiogram, SQLAlchemy, Alembic, Pytest, Numpy
- **TypeScript**: NestJS, Express, Grammy, Prisma, Drizzle ORM
- **DevOps**: Git, Docker, Nginx

## 🚀 Быстрый старт

### Требования
- **Node.js** 16+ (для фронтенда)
- **npm** или **yarn**
- **Git**

### Установка и запуск фронтенда

1. **Клонируем репозиторий**:
```bash
git clone https://github.com/Keva1z/portfolio.git
cd portfolio
```

2. **Переходим в папку клиента**:
```bash
cd client
```

3. **Устанавливаем зависимости**:
```bash
npm install
```

4. **Запускаем dev сервер**:
```bash
npm run dev
```

Откроется на **http://localhost:5173** (или другом порту, указанном в терминале)

### Слайды:
1. **Hero** — приветствие с портретом
2. **Обо мне** — описание, tech stack
3. **Проекты** — NeforLove и другие проекты
4. **Мысли** — автозагрузка мыслей из Telegram-бота
5. **Контакты** — ссылки на GitHub и Telegram

## 🔧 Конфигурация

### Подключение к бэкенду

В файле `src/components/Thoughts.jsx` найди строку с API URL:

```javascript
const fetchThoughts = async () => {
  const response = await fetch('https://ваш-бэкенд.ru/api/thoughts');
  // ...
};
```

Замени `'https://ваш-бэкенд.ru/api/thoughts'` на адрес своего бэкенда.

### Требуемый формат API ответа

GET `/api/thoughts` должен возвращать:
```json
{
  "thoughts": [
    {
      "id": "uuid-или-строка",
      "text": "Текст мысли",
      "createdAt": "2024-01-20T15:30:00Z"
    }
  ]
}
```

## 📁 Структура проекта

```
portfolio/
├── client/                    # React приложение
│   ├── public/
│   │   ├── images/           # Изображения (портрет и т.д.)
│   │   └── videos/           # Видео фон
│   ├── src/
│   │   ├── components/
│   │   │   ├── BackgroundVideo.jsx
│   │   │   └── Thoughts.jsx
│   │   ├── App.jsx           # Главный компонент
│   │   ├── index.css         # Глобальные стили
│   │   └── main.jsx          # Точка входа
│   ├── package.json
│   └── vite.config.js
├── docker-compose.yml        # Docker конфигурация (опционально)
└── README.md

```

## 🎨 Как работает фронтенд

### BackgroundVideo
Компонент с фиксированным видео фоном и overlay градиентом.

### Thoughts
Автоматически загружает мысли с бэкенда каждые 30 секунд:
- Отображает загрузку, ошибки, пустое состояние
- Форматирует даты в формате `DD.MM.YYYY HH:MM`
- Сортирует мысли по дате (новые сверху)
- Кнопка retry для ручной переподгрузки

### Навигация
Использует IntersectionObserver для отслеживания видимости элементов и Keyboard Events для управления через клавиатуру.

## 📝 Скрипты

```bash
# Развитие
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр собранного проекта
npm run preview

# Проверка кода линтером
npm run lint
```

## 🔌 Подготовка к бэкенду

Проект готов к интеграции с бэкенд-ом. Для полной функциональности нужен бэкенд с:

1. **GET /api/thoughts** — получение всех мыслей
2. **POST /api/thoughts** — сохранение новой мысли (из Telegram-бота)
3. **CORS** — разрешить запросы с домена портфолио

## 📞 Контакты

- **GitHub**: https://github.com/Keva1z
- **Telegram**: https://t.me/Keva1z
- **NeforLove**: https://nefor-love.ru

## 📄 Лицензия

MIT License — свободен для использования и модификации.
